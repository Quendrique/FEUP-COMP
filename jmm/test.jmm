class Fac {
	public int ComputeFac(int num, int asd){
		int num_aux ;
		if (num < 1)
			num_aux = 1;
		else
			num_aux = num * (this.ComputeFac(num-1));
		a[0]=b;
		while (i + - 10) {
			
		}
		return num_aux;
	}
	public static void main(String[] a){
		io.println(new Fac().ComputeFac(10)); //hmm
	}
}